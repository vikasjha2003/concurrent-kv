# concurrent-kv
A concurrent in-memory key-value database built from scratch in modern C++, featuring TCP networking, thread-based request processing, TTL expiration, LRU eviction, persistence, and performance benchmarking.
